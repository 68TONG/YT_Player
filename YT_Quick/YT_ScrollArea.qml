import QtQuick

MouseArea {

    hoverEnabled: true

    property int v: 0
    onPressed: (mouse) => {

               }
    onPositionChanged: (mouse) => {
                       }
    onReleased: (mouse) => {

                }
}
